- унарный минус на текущий момент не поддерживается
- числа обрабатываются как double
- максимальная длина числа - 100 цифр, этого вполне должно хватить, учитывая точность double
- калькулятор поддерживает вызов функций одного аргумента, которые можно описать в functions.c
- программа читает выражение, которое необходимо вычислить с stdin до получения EOF, и выводит результат на stdout, в формате %.9lf
- максимальным приоритетом обладает вызов функций, затем ^, * и /, затем - и +
- варианты вызова функции на примере sqr:
    sqr(3)
    sqr 3
    sqr3
- если запустить программу как "./calc opz", то калькулятор будет принимать только обратную польскую нотацию
- в стандартном режиме он понимает как обычную, так и ОПН.
- обработки _вычислительных_ ошибок нет, полагаюсь на C-шную обработку, "echo 1/0 | ./calc" возвращает "inf", 0/0 - nan
