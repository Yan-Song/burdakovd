function I = loadKeepingSomeClasses(filename, classes)
    I = imread(filename);
    h = size(I, 1);
    w = size(I, 2);
    ok = I == 255;
    
    for i = classes
        ok = ok || 
    end
end